#En la raíz de Api cree un archivo .env y copie el contenido que está fijado en whats <3
#Ese .env no se subirá aunque se les olvide borrar, este .env.production es un recordatorio para que no se suba el .env real, 
#y dónde encontrar la info